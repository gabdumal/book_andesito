---
title: Instalação
author: Gabriel Malosto
---

import Figure from "/src/components/Figure.astro";

import downloadOptions from "/src/assets/content/docs/c_cpp/cmake/install/download_options.png";
import installingWindows from "/src/assets/content/docs/c_cpp/cmake/install/installing_windows.png";
import checkingInstallationWindows from "/src/assets/content/docs/c_cpp/cmake/install/checking_installation_windows.png";
import ninjaDownloadOptions from "/src/assets/content/docs/c_cpp/cmake/install/ninja_download_options.png";

A instalação do **CMake** é simples e pode ser feita de várias formas, dependendo do sistema operacional.

Vamos também instalar o **Ninja**, um gerador de build que é mais rápido que o **Make**.

## Linux

{/* TODO */}

## Windows

Para instalar o **CMake** no Windows, acesse a página de [downloads](https://cmake.org/download/) do CMake.

Então, selecione a opção do instalador para Windows x64 mais recente.

<Figure
  caption="Opções de download do CMake."
  images={[
    {
      src: downloadOptions,
      alt: "Página de downloads do website do CMake. A versão mais recente para Windows x64 está destacada.",
    },
  ]}
/>

Execute o instalador e siga as instruções.
Na página **Install Options**, selecione a opção **Add CMake to the PATH environment variable**.
Então prossiga com a instalação.

<Figure
  caption="Instalando o CMake no Windows."
  images={[
    {
      src: installingWindows,
      alt: `Instalador do CMake no Windows, em que se seleciona a opção "Add CMake to the PATH environment variable".`,
    },
  ]}
/>

Para verificar se a instalação foi bem-sucedida, abra o PowerShell e execute o comando `cmake`.

<Figure
  caption="Verificando se a instalação do CMake no Windows foi bem-sucedida."
  images={[
    {
      src: checkingInstallationWindows,
      alt: "Terminal com o PowerShell, em que se executa o comando supracitado.",
    },
  ]}
/>

### Ninja

Para instalar o **Ninja**, acesse a página de [downloads](https://github.com/ninja-build/ninja/releases).

Selecione a opção para Windows na versão mais recente, marcada como **latest**.

<Figure
  caption="Opções de download do Ninja."
  images={[
    {
      src: ninjaDownloadOptions,
      alt: "Página de downloads do Ninja pelo GitHub. A versão mais recente para Windows está destacada.",
    },
  ]}
/>

Extraia o arquivo Zip baixado.
Então rode o executável `ninja.exe`.

{/* TODO: Continuar */}
