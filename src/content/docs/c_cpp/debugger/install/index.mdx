---
title: Instalação
author: Gabriel Malosto
---

import Figure from "/src/components/Figure.astro";

import llvmDownloadOptions from "/src/assets/content/docs/c_cpp/debugger/install/llvm_download_options.png";
import installingLlvm from "/src/assets/content/docs/c_cpp/debugger/install/installing_llvm.png";
import testingInstallation from "/src/assets/content/docs/c_cpp/debugger/install/testing_installation.png";

O LLDB depende de ter instalado o [Python](../python).
Em sistemas Linux (incluindo WSL), ao fazer a instalação do LLDB, todas as dependências são resolvidas e instaladas.
Já para o Windows, cada dependência deve ser manualmente instalada previamente.

## Linux

### Ubuntu e WSL

```bash
sudo apt update && sudo apt upgrade
sudo apt install wget
sudo bash -c "$(wget -O - https://apt.llvm.org/llvm.sh)"
sudo apt --fix-broken install
sudo apt install llvm lldb
```

### Fedora

{/* TODO */}

## Windows

Na data de escrita deste livro, para instalar o **LLDB** no Windows, é necessário ter instalado especificamente o **Python 3.10**.
Leia o capítulo sobre a linguagem [**Python**](../python) para saber como instalá-la.

### LLVM

Instalar o LLVM no Windows é um pouco mais complicado do que no Linux.
A forma mais fácil de fazê-lo é baixar o instalador do projeto **LLVM** do [repositório oficial](https://github.com/llvm/llvm-project).

A fim de evitar problemas de compatibilidade de versões, recomendo instalar a versão mais recente na data de escrita deste livro, que é a **19.1.2**.
Assim, eu lhes garanto que o processo de instalação funcionará com o Python 3.10.

Acesse a página de [**downloads do LLVM**](https://github.com/llvm/llvm-project/releases/tag/llvmorg-19.1.2) na versão 19.1.2 e baixe o arquivo `LLVM-19.1.2-win64.exe`.

<Figure
  caption="Opções de download do LLVM."
  images={[
    {
      src: llvmDownloadOptions,
      alt: `Página de downloads do website do LLVM. A versão "LLVM-19.1.2-win64.exe" está destacada.`,
    },
  ]}
/>

Então, execute o instalador e siga as instruções.
Na página **Install Options**, selecione a opção **Add LLVM to the system PATH for all users**.
Então prossiga com a instalação.

<Figure
  caption={`Instalador do LLVM na página "Install Options".`}
  images={[
    {
      src: installingLlvm,
      alt: `Instalador do LLVM na página "Install Options". São presentadas três opções, sendo selecionada a segunda: "Add LLVM to the system PATH for all users".`,
    },
  ]}
/>

Para verificar se a instalação foi bem-sucedida, abra uma nova instância do PowerShell e execute o comando `lldb --version`.

<Figure
  caption="Verificando se a instalação do LLVM foi bem-sucedida."
  images={[
    {
      src: testingInstallation,
      alt: `Terminal com o PowerShell, em que se executa o comando supracitado.`,
    },
  ]}
/>
