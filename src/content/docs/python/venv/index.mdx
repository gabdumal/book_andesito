---
title: Ambientes virtuais
author: Gabriel Malosto
---

import Figure from "@components/Figure.astro";

import installingVenvPowerShell from "@assets/content/docs/python/venv/installing_venv_powershell.png";

# Ambientes virtuais

```powershell
pip install virtualenv
```

<Figure
  caption="Instalando o pacote `virtualenv` no PowerShell"
  images={[
    {
      src: installingVenvPowerShell,
      alt: "Terminal com o PowerShell, em que se executa o comando para instalar o pacote `virtualenv`",
    },
  ]}
/>

{/* TODO: Criar exemplo para ensinar a usar o venv */}
