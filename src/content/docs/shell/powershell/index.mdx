---
title: PowerShell
author: Gabriel Malosto
---

import { Kbd } from "starlight-kbd/components";

import Figure from "@components/Figure.astro";

import installing7 from "@assets/content/docs/shell/powershell/installing_7.png";
import hideWindowsPowerShell from "@assets/content/docs/shell/powershell/hide_windows_powershell.png";

O **PowerShell** é uma ferramenta de linha de comando desenvolvida pela Microsoft, que é utilizada para automatizar tarefas e gerenciar configurações do sistema operacional.

Por padrão, o Windows vem com o **PowerShell 5.1** instalado.
No entanto, a versão mais recente é a [**PowerShell 7**](https://learn.microsoft.com/en-us/powershell/scripting/whats-new/migrating-from-windows-powershell-51-to-powershell-7), que traz diversas melhorias e novas funcionalidades.

Segundo a Microsoft, ambas as versões podem existir lado a lado no mesmo sistema, sem conflitos.
Neste capítulo, vamos mostrar como instalar o **PowerShell 7** no Windows.

## Instalando o PowerShell 7

O modo de instalação recomendado pela Microsoft é através do **WinGet**, que é um gerenciador de pacotes para o Windows.
Você pode acessar seu [guia de instalação](https://learn.microsoft.com/en-us/powershell/scripting/install/installing-powershell-on-windows?view=powershell-7.4#install-powershell-using-winget-recommended).

Segundo ele, o comando a ser executado é:

```powershell title="PowerShell 5.1" frame="terminal"
winget install --id Microsoft.PowerShell --source winget
```

<Figure
  caption="Processo de instalação do PowerShell 7 pelo WinGet."
  images={[
    {
      src: installing7,
      alt: "Terminal do PowerShell, em que se executa o comando de instalação supracitado.",
    },
  ]}
/>

**Feche** a abra o PowerShell para que as alterações tenham efeito.

Abra as configurações do Windows Terminal com o comando <Kbd linux={"Ctrl+,"} />

Você verá que um novo perfil, para o PowerShell 7, foi instalado, chamado simplesmente de **PowerShell** e com ícone preto.

Podemos, então, ocultar a exibição do perfil do PowerShell 5.1.
Ele é identificado na entrada `Windows PowerShell`, com ícone azul.
Selecione-a e ative a opção `Ocultar perfil da lista suspensa`.

<Figure
  caption="Escondendo o Windows Powershell."
  images={[
    {
      src: hideWindowsPowerShell,
      alt: `Configurações do perfil "Windows Powershell", em que a opção de escondê-lo está ativa.`,
    },
  ]}
/>

Salve, **feche** e abra o Windows Terminal novamente para que as alterações tenham efeito.
