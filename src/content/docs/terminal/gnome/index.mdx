---
title: GNOME Terminal
author: Gabriel Malosto
---

import { Kbd } from "starlight-kbd/components";

import Figure from "/src/components/Figure.astro";

import searchingTerminalOnUbuntu from "/src/assets/content/docs/terminal/gnome/searching_terminal_on_ubuntu.png";
import openingTerminalFromTerminal from "/src/assets/content/docs/terminal/gnome/opening_terminal_from_terminal.gif";
import openingKeyboardSettings from "/src/assets/content/docs/terminal/gnome/opening_keyboard_settings.png";
import settingTerminalShortcut from "/src/assets/content/docs/terminal/gnome/setting_terminal_shortcut.png";
import openingPreferences from "/src/assets/content/docs/terminal/gnome/opening_preferences.png";
import preferences from "/src/assets/content/docs/terminal/gnome/preferences.png";
import profile from "/src/assets/content/docs/terminal/gnome/profile.png";
import installingCatppuccin from "/src/assets/content/docs/terminal/gnome/installing_catppuccin.png";
import settingTheme from "/src/assets/content/docs/terminal/gnome/setting_theme.png";
import changedFontSize from "/src/assets/content/docs/terminal/gnome/changed_font_size.png";
import result from "/src/assets/content/docs/terminal/gnome/result.png";

Este subcapítulo aborda o **GNOME Terminal**, o emulador de terminal padrão do **Mint**, do **Ubuntu**, do **Debian** e de muitas outras distribuições.

Pesquise por `Terminal` no menu do seu sistema operacional e clique no ícone do aplicativo para abri-lo.

<Figure
  caption="Buscando pelo Terminal no menu de apps do GNOME."
  images={[
    {
      src: searchingTerminalOnUbuntu,
      alt: `Menu de apps do GNOME no Mint, em que se busca por "terminal".`,
    },
  ]}
/>

O GNOME terminal também pode ser aberto por um comando!
Digite `gnome-terminal` no terminal e pressione <Kbd linux={"Enter"}/>.

<Figure
  caption="Abrindo o GNOME Terminal a partir do terminal."
  images={[
    {
      src: openingTerminalFromTerminal,
      alt: "Terminal do GNOME, em que se executa o comando para abrir outro terminal.",
    },
  ]}
/>

## Atalho de teclado

Um atalho de teclas comumente utilizado para abrir o terminal é <Kbd linux={"Ctrl+Alt+T"}/>.
O Mint o tem configurado por padrão.
Já no Fedora, você pode defini-lo em `Settings > Keyboard > View and Customize Shortcuts > Custom Shortcuts`.

<Figure
  caption="Selecionando menu de atalhos de teclado nas configurações do GNOME."
  images={[
    {
      src: openingKeyboardSettings,
      alt: "Configurações do GNOME, em que está aberto o menu de atalhos de teclado.",
    },
  ]}
/>

Então, defina o nome como `Terminal`, o comando como `gnome-terminal` e o atalho de teclado como <Kbd linux={"Ctrl+Alt+T"}/>.
Pressione `Add` e pronto!

<Figure
  caption="Definindo o atalho de teclado para abrir o terminal."
  images={[
    {
      src: settingTerminalShortcut,
      alt: "Configurações do GNOME, em que se define o atalho de teclado para abrir o terminal.",
    },
  ]}
/>

## Perfis

O GNOME Terminal permite a criação de perfis, que são configurações personalizadas para o terminal.

Abra o menu do terminal e selecione `Preferences`.

<Figure
  caption="Abrindo o menu de preferências do GNOME Terminal."
  images={[
    {
      src: openingPreferences,
      alt: "GNOME Terminal, em que se seleciona o botão de menu no canto superior direito.",
    },
  ]}
/>

Eu prefiro um tema claro, então selecionei `Light style` na opção de variante de tema.

<Figure
  caption="Selecionando a variante de tema clara."
  images={[
    {
      src: preferences,
      alt: "Preferências do GNOME Terminal, em que se seleciona a variante de tema clara.",
    },
  ]}
/>

Por padrão, o GNOME Terminal tem um perfil chamado `Unnamed`.
Clicando nele, podemos ver as definições padrão.

<Figure
  caption={`Definições padrão do perfil "Unnamed".`}
  images={[
    {
      src: profile,
      alt: `Preferências do GNOME Terminal na seção de perfis, em que se mostra as definições padrão do perfil "Unnamed".`,
    },
  ]}
/>

Você pode alterar todas as configurações do terminal, como fonte, cores, cursor, etc.
Porém, é meio trabalhoso fazer tudo isso.

Então, eu gosto de usar um tema bastante popular chamado `Catppuccin`!
Ele é um projeto que oferece personalização para uma série de programas.
Você pode optar entre quatro variantes: Latte, Frappé, Macchiato e Mocha.

Para instalar o tema, execute o script de instalação oferecido no [repositório do projeto](https://github.com/catppuccin/gnome-terminal?tab=readme-ov-file#usage).

```bash
curl -L https://raw.githubusercontent.com/catppuccin/gnome-terminal/v0.3.0/install.py | python3 -
```

<Figure
  caption="Instalando o tema Catppuccin para o GNOME Terminal."
  images={[
    {
      src: installingCatppuccin,
      alt: "Terminal do GNOME, em que se executa o script de instalação do tema Catppuccin.",
    },
  ]}
/>

Depois de instalado, abra as preferências do terminal e selecione o perfil que mais lhe agrada.
Eu gosto do `Catppuccin Latte`.

Para isso, clique no botão `▾` ao lado do nome do tema, e selecione `Set as default`.

<Figure
  caption="Definindo o tema Catppuccin Latte como padrão."
  images={[
    {
      src: settingTheme,
      alt: "Preferências do GNOME Terminal, em que se define o tema Catppuccin Latte como padrão.",
    },
  ]}
/>

Por último, eu gosto de aumentar o tamanho da fonte para 16.

Clique na caixa de seleção com a legenda `Custom font:`, e então clique no botão ao lado, com o texto `Monospace 12`.

Você pode então selecionar sua fonte de preferência, e o tamanho dela.

<Figure
  caption="Definições do perfil após mudar o tamanho da fonte."
  images={[
    {
      src: changedFontSize,
      alt: "Preferências do GNOME Terminal, em que se altera o tamanho da fonte.",
    },
  ]}
/>

Para carregar as modificações, feche e abra o terminal novamente.

<Figure
  caption="Aparência final do terminal após mudar o tema."
  images={[
    {
      src: result,
      alt: "Terminal do GNOME com o tema Catppuccin Latte aplicado.",
    },
  ]}
/>
