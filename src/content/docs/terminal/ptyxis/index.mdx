---
title: Ptyxis
author: Gabriel Malosto
---

import { Kbd } from "starlight-kbd/components";

import Figure from "/src/components/Figure.astro";

import openingKeyboardSettings from "/src/assets/content/docs/terminal/ptyxis/opening_keyboard_settings.png";
import settingTerminalShortcut from "/src/assets/content/docs/terminal/ptyxis/setting_terminal_shortcut.png";
import ptyxisMenu from "/src/assets/content/docs/terminal/ptyxis/ptyxis_menu.png";
import settingTheme from "/src/assets/content/docs/terminal/ptyxis/setting_theme.png";
import selectingFiraCode from "/src/assets/content/docs/terminal/ptyxis/selecting_firacode.png";

Este subcapítulo aborda o **Ptyxis**, o emulador de terminal padrão do **Fedora**.

## Atalho de teclado

Um atalho de teclas comumente utilizado para abrir o terminal é <Kbd linux={"Ctrl+Alt+T"}/>.
O Mint o tem configurado por padrão.
Já no Fedora, você pode defini-lo no aplicativo de configurações.
Pesquise por `Settings` no menu do seu sistema operacional e clique no ícone do aplicativo para abri-lo.

Então, na barra lateral, selecione `Keyboard`.
No menu que se abrir, desça a tela e clique na opção `View and Customize Shortcuts`.
Então, selecione `Custom Shortcuts`.

<Figure
  caption="Selecionando menu de atalhos de teclado nas configurações do GNOME."
  images={[
    {
      src: openingKeyboardSettings,
      alt: "Configurações do GNOME, em que está aberto o menu de atalhos de teclado.",
    },
  ]}
/>

Então, defina o nome como `Terminal`, o comando como `ptyxis` e o atalho de teclado como <Kbd linux={"Ctrl+Alt+T"}/>.
Pressione `Add` e pronto!

<Figure
  caption="Definindo o atalho de teclado para abrir o terminal."
  images={[
    {
      src: settingTerminalShortcut,
      alt: "Configurações do GNOME, em que se define o atalho de teclado para abrir o terminal.",
    },
  ]}
/>

## Personalização

O Ptyxis já traz várias configurações de personalização.
Clique no botão de menu no canto superior direito da janela do terminal.

### Estilo

A primeira coisa que você pode fazer é mudar o **estilo** de cores do terminal.
Eu prefiro deixá-lo seguir o estilo do sistema.
Para isso, selecione o primeiro círculo, que apresenta uma metade em branco e outra em preto.

Se preferir um tema **escuro**, selecione o círculo que tem um fundo preto.
O estilo de cores do terminal influencia na lista de temas de cores disponíveis, então escolhe um deles de antemão.

<Figure
  caption="Definindo o estilo de cores do terminal."
  images={[
    {
      src: ptyxisMenu,
      alt: "Menu do terminal Ptyxis, em que se define o estilo de cores do terminal.",
    },
  ]}
/>

### Cores

Para mais opções de personalização, clique na entrada `Preferences` do menu.
A primeira aba, chamada `Appearance`, permite escolher o tema de cores do terminal.

Eu gosto de usar um chamado `Catppuccin Latte`.
Para que ele seja exibido, é necessário clicar no botão `Show all palettes`, no canto superior direito da janela.

<Figure
  caption="Definindo o estilo de cores do terminal."
  images={[
    {
      src: settingTheme,
      alt: `Configurações do terminal Ptyxis, na aba "Appearance", em que se seleciona o tema "Catppuccin Latte".`,
    },
  ]}
/>

Se você tiver selecionado um estilo claro, apenas aparecerão temas claros.
O mesmo vale para os temas escuros.
Então, se você não estiver vendo o tema que deseja, altere o estilo de cores do terminal.

### Fonte

Agora, vamos aplicar a fonte do padrão NerdFonts que instalamos.
Desça a tela até a seção `Font` nessa mesma aba `Appearance`.
Desative a opção `Use system font`.
Então, clique na opção `Custom Font`.

Na janela que se abrir, selecione a fonte `FiraCode Nerd Font Mono Regular` e clique em `Select`.

<Figure
  caption="Menu de seleção de fonte."
  images={[
    {
      src: selectingFiraCode,
      alt: `Menu de seleção de fontes do GNOME Terminal, em que se busca pelo texto "FiraCode", e se seleciona a fonte "FiraCode Nerd Font Mono Regular".`,
    },
  ]}
/>
